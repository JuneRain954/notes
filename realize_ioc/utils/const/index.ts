/**
 * 常量
 */

// 定义元数据时使用的 key
export enum KEY {
  PATH = "ico:path",
  METHOD = "ioc:method",
  TYPE = "design:type",
};

// 请求方式
export enum REQ_METHOD {
  GET = "GET",
  POST = "POST"
};

// 分隔符
export const DELIMITER = ":";